<%= form_for(@screen_slider) do |f| %>
  <% if @screen_slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@screen_slider.errors.count, "error") %> prohibited this screen_slider from being saved:</h2>

      <ul>
      <% @screen_slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
